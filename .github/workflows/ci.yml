name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

# Cancel superseded runs on the same ref to save runner minutes.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  ZIG_VERSION: "0.16.0"
  # Pinned upstream SHA256 (x86_64-linux), see https://ziglang.org/download/index.json
  ZIG_SHA256_X86_64: "70e49664a74374b48b51e6f3fdfbf437f6395d42509050588bd49abe52ba3d00"
  # Pinned upstream SHA256 (aarch64-macos), same source.
  ZIG_SHA256_AARCH64_MACOS: "b23d70deaa879b5c2d486ed3316f7eaa53e84acf6fc9cc747de152450d401489"

jobs:
  unit-test:
    name: Unit tests (zig build test)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Zig ${{ env.ZIG_VERSION }}
        run: |
          set -euo pipefail
          url="https://ziglang.org/download/${ZIG_VERSION}/zig-x86_64-linux-${ZIG_VERSION}.tar.xz"
          curl -fsSL "$url" -o /tmp/zig.tar.xz
          echo "${ZIG_SHA256_X86_64}  /tmp/zig.tar.xz" | sha256sum -c -
          sudo mkdir -p /opt/zig
          sudo tar -xJf /tmp/zig.tar.xz -C /opt/zig --strip-components=1
          sudo ln -s /opt/zig/zig /usr/local/bin/zig
          zig version

      - name: zig build test
        run: zig build test --summary all

      - name: zig build tools-test
        run: zig build tools-test --summary all

      - name: Enforce one-way tools dependency (src/ must not import tools/)
        run: |
          set -euo pipefail
          if grep -rn '@import("[^"]*tools/' src/; then
            echo "ERROR: the data plane under src/ must not import anything from tools/ (issue #57)." >&2
            exit 1
          fi
          echo "OK: no src/ -> tools/ imports."

  # macOS is a first-class spoke platform (docs/macos-spoke-acceptance.md), so
  # its unit-test baseline is gated here, not left to ad-hoc local runs. Unit
  # tests only: the netns/TUN integration harness is Linux-only by design.
  unit-test-macos:
    name: Unit tests (macOS)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Zig ${{ env.ZIG_VERSION }}
        run: |
          set -euo pipefail
          url="https://ziglang.org/download/${ZIG_VERSION}/zig-aarch64-macos-${ZIG_VERSION}.tar.xz"
          curl -fsSL "$url" -o /tmp/zig.tar.xz
          echo "${ZIG_SHA256_AARCH64_MACOS}  /tmp/zig.tar.xz" | shasum -a 256 -c -
          sudo mkdir -p /opt/zig
          sudo tar -xJf /tmp/zig.tar.xz -C /opt/zig --strip-components=1
          sudo ln -s /opt/zig/zig /usr/local/bin/zig
          zig version

      - name: zig build test
        run: zig build test --summary all

      - name: zig build tools-test
        run: zig build tools-test --summary all

  cross-build:
    name: Cross-build ${{ matrix.arch }} (static + size budget)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            target: x86_64-linux-musl
            cpu: ""
          - arch: arm64
            target: aarch64-linux-musl
            cpu: ""
          - arch: armv7
            target: arm-linux-musleabihf
            cpu: "generic+v7a"
          - arch: armv5
            target: arm-linux-musleabi
            cpu: "arm926ej_s"
          - arch: mipsel
            target: mipsel-linux-musl
            cpu: ""
            qemu: "qemu-mipsel-static"
          - arch: mips
            target: mips-linux-musl
            cpu: ""
            qemu: "qemu-mips-static"
    steps:
      - uses: actions/checkout@v5

      - name: Install Zig ${{ env.ZIG_VERSION }}
        run: |
          set -euo pipefail
          url="https://ziglang.org/download/${ZIG_VERSION}/zig-x86_64-linux-${ZIG_VERSION}.tar.xz"
          curl -fsSL "$url" -o /tmp/zig.tar.xz
          echo "${ZIG_SHA256_X86_64}  /tmp/zig.tar.xz" | sha256sum -c -
          sudo mkdir -p /opt/zig
          sudo tar -xJf /tmp/zig.tar.xz -C /opt/zig --strip-components=1
          sudo ln -s /opt/zig/zig /usr/local/bin/zig
          zig version

      - name: Build + assert static and <=512KB
        run: |
          set -euo pipefail
          size_budget=524288
          cpu_arg=""
          if [ -n "${{ matrix.cpu }}" ]; then cpu_arg="-Dcpu=${{ matrix.cpu }}"; fi
          zig build -Dtarget="${{ matrix.target }}" $cpu_arg -Doptimize=ReleaseSmall
          for bin in subnetrad subnetra; do
            path="zig-out/bin/${bin}"
            if readelf -d "$path" 2>/dev/null | grep -q NEEDED; then
              echo "ERROR: ${path} is dynamically linked" >&2; exit 1
            fi
            size=$(stat -c%s "$path")
            echo "${bin} (${{ matrix.arch }}): ${size} bytes"
            if [ "$size" -gt "$size_budget" ]; then
              echo "ERROR: ${path} ${size} bytes exceeds ${size_budget}" >&2; exit 1
            fi
          done

      # Build-only proves it LINKS; this proves the cross binary actually EXECUTES
      # on the target ISA (musl startup, atomic relocations, JSON parse, peer
      # registry build, config validation) on both endiannesses. `--version` and
      # `--check` are offline control-path ops with no TUN/socket/timing, so
      # qemu-user runs them deterministically — unlike the data-plane e2e, which
      # we deliberately never emulate (see test/integration/run.sh) because qemu
      # would skew its throughput/latency measurements.
      - name: Execute smoke under qemu-user (MIPS)
        if: matrix.qemu != ''
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y -qq qemu-user-static
          ${{ matrix.qemu }} zig-out/bin/subnetra  --version
          ${{ matrix.qemu }} zig-out/bin/subnetrad --version
          ${{ matrix.qemu }} zig-out/bin/subnetrad --check --config deploy/hub.json
  integration:
    name: Integration + e2e (static binary, size budget, netns relay)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Zig ${{ env.ZIG_VERSION }}
        run: |
          set -euo pipefail
          url="https://ziglang.org/download/${ZIG_VERSION}/zig-x86_64-linux-${ZIG_VERSION}.tar.xz"
          curl -fsSL "$url" -o /tmp/zig.tar.xz
          echo "${ZIG_SHA256_X86_64}  /tmp/zig.tar.xz" | sha256sum -c -
          sudo mkdir -p /opt/zig
          sudo tar -xJf /tmp/zig.tar.xz -C /opt/zig --strip-components=1
          sudo ln -s /opt/zig/zig /usr/local/bin/zig
          zig version

      # The harness builds both arch targets, asserts static + <=512KB, then runs
      # the privileged multi-point + relay netns e2e (needs root + /dev/net/tun,
      # both available on GitHub-hosted ubuntu runners). It SKIPs (not fails) any
      # piece whose prerequisite is missing.
      - name: Load tun module
        run: sudo modprobe tun

      - name: Run integration harness
        run: sudo --preserve-env=PATH env "PATH=$PATH" bash test/integration/run.sh
